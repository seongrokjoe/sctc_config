<UserControl x:Class="SCTC_CONFIG.Views.CsvTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:SCTC_CONFIG.Views">
    <Grid Background="#EDF2F7">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <views:MainPanelView Grid.Column="0" DataContext="{Binding MainPanel}"/>
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                      Background="#CBD5E0" ResizeBehavior="PreviousAndNext"/>
        <views:DriverPanelView Grid.Column="2" DataContext="{Binding DriverPanel}"/>
        <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"
                      Background="#CBD5E0" ResizeBehavior="PreviousAndNext"/>
        <views:FunctionPanelView Grid.Column="4" DataContext="{Binding FunctionPanel}"/>
        <GridSplitter Grid.Column="5" Width="5" HorizontalAlignment="Stretch"
                      Background="#CBD5E0" ResizeBehavior="PreviousAndNext"/>
        <views:AlarmPanelView Grid.Column="6" DataContext="{Binding AlarmPanel}"/>
    </Grid>
</UserControl>
